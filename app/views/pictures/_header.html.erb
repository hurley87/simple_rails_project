<h1 class="pull-left">Picture App</h1>

<div class="pull-right">
	<% if user_signed_in? %>
		<% link_to "Log Out", destroy_user_session_path %>
	<% else %>
	<h3><%= link_to "Sign Up", new_user_registration_path %> | </h3>
	<% end %>
</div>